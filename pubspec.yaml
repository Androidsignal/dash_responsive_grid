name: dash_responsive_grid
description: "Lightweight, dependency-free responsive grid for Flutter. Auto column count by screen width, a staggered masonry layout, and loading/empty/error states."
version: 0.0.2
homepage: https://github.com/dashstack/dash_responsive_grid
repository: https://github.com/dashstack/dash_responsive_grid
issue_tracker: https://github.com/dashstack/dash_responsive_grid/issues
topics:
  - grid
  - responsive
  - layout
  - masonry

environment:
  sdk: ^3.13.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
