name: dash_responsive_grid
description: "Lightweight, dependency-free responsive grid widget for Flutter. Auto column count by screen width, with loading/empty/error states."
version: 0.0.1
homepage: https://github.com/dashstack/dash_responsive_grid

environment:
  sdk: ^3.13.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
